// main.c
#include <stdio.h>
#include "triple.h" // Incluir el archivo de cabecera

int main() {
    int limite = 20; // Limite para buscar triples pitagóricos
    encontrarTriplesPitagoricos(limite); // Llamada a la función que busca triples
    return 0;
}
